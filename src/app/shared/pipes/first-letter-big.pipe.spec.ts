import { FirstLetterBigPipe } from './first-letter-big.pipe';

describe('FirstLetterBigPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstLetterBigPipe();
    expect(pipe).toBeTruthy();
  });
});
